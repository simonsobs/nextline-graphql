type Query {
    hello: String!
    state: State!
}

type Subscription {
    counter: Int!
    state: State!
    stdout: String
}

type Mutation {
    exec: Boolean!
    reset: Boolean!
    sendPdbCommand(threadId: String!, taskId: String, command: String!): Boolean!
}

type Task {
    taskId: String,
    prompting: Int,
    fileName: String,
    lineNo: Int,
    fileLines: [String]
}

type Thread {
    threadId: String!
    tasks: [Task]
}

type State {
    state: String!
    nthreads: Int
    threads: [Thread]
}
