# A configuration file of Dynaconf
# 
# To be used when creating migration versions.
# 
# In ../config.py, it is set up so this config file will be used when the cwd
# is this directory.

db = {url = "sqlite:///migration.sqlite3"}
